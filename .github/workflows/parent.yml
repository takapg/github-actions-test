name: parent

on:
  push:
    branches:
      - main

jobs:
  main:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: ["dev", "stage", "prod"]
    uses: ./.github/workflows/child.yml@main
    with:
      target: ${{ matrix.target }}
