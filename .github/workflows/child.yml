name: child

on:
  workflow_call:
    inputs:
      paths:
        required: true
        type: string

jobs:
  main:
    runs-on: ubuntu-latest
    # strategy:
    #   matrix:
    #     path:${{ fromJson(inputs.target).paths }}
    steps:
      - run: echo ${{ inputs.paths }}
      # - run: echo ${{ matrix.path }}
