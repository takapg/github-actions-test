name: child

on:
  workflow_call:
    inputs:
      paths:
        required: true
        type: string

jobs:
  main:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        path: ${{ fromJson(inputs.paths) }}
    steps:
      - run: echo ${{ matrix.path }}
