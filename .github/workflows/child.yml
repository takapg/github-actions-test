name: child

on:
  workflow_call:
    inputs:
      target:
        required: true
        type: string

jobs:
  main:
    runs-on: ubuntu-latest
    # strategy:
    #   matrix:
    #     path:${{ fromJson(inputs.target).paths }}
    steps:
      - run: echo ${{ inputs.target }}
      # - run: echo ${{ matrix.path }}
